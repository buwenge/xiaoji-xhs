# 复制成 .env 后按需取消注释修改；都不填也能跑，默认值就是下面写的（~ 指当前用户家目录，写进 .env 时要写绝对路径）。

# 本仓库所在目录（bin/home 和 daemon_api 用来找 .web_auth_token）
# XIAOJI_HOME=/opt/xiaoji-xhs

# 小红书状态目录：登录态、下载的图、读图缓存都在这里。
# 必须在机器人仓库外面，否则读图子进程会把你机器人的 CLAUDE.md 一起吃进去。
# XHS_DIR=/home/你/.xhs

# 读图模型：默认 sonnet 批量一句话描述；"读图 N 仔细/全文"用 opus
# XHS_VISION_MODEL=sonnet
# XHS_VISION_DETAIL_MODEL=opus

# 无头 claude -p 用的长期令牌文件（内容一行：CLAUDE_CODE_OAUTH_TOKEN=...），
# 用 `claude setup-token` 生成。不填就用 claude 当前的登录态。
# CLAUDE_OAUTH_TOKEN_FILE=/home/你/.claude-oauth-token.env

# 你的机器人 daemon 的本地 HTTP 地址（发原图/截图/分享链接时调用）
# HOME_DAEMON_API_BASE=http://127.0.0.1:3000

# 看守浏览器空闲多少秒自动关（默认 600）
# XHS_KEEPER_IDLE_TIMEOUT=600

# Chrome 远程调试端口（默认 9223）
# XHS_CDP_PORT=9223
